import 'package:flutter/material.dart';

class WeatherMapWebView extends StatelessWidget {
  const WeatherMapWebView({Key? key}) : super(key: key);
  static const routeName = 'weatherMap';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
